<td{% if (footerCell.align is not null) %} style="text-align: {{ footerCell.align }}"{% endif %}{% for key, value in (footerCell.attributes) %} {{ key }}="{{ value }}"{% endfor %}>
{{ footerCell.value|default('')|raw }}
</td>
