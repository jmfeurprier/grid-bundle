{% if (grid.rows is not empty) %}
    <table>
        {{ include('grid_header.html.twig') }}
        {{ include('grid_body.html.twig') }}
        {{ include('grid_footer.html.twig') }}
    </table>
{% endif %}
